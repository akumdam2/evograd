"""Level 1 — the reusable primitives Llama-3-8B executes.

The contracts live in :mod:`evograd.ops.level1`; :mod:`manifest` records which
of them this model runs, which Level-2 site each composes into, and how this
model's observed cases bind onto the shared contracts.
"""
