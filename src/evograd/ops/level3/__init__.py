"""Legacy direct-block operator tasks.

These declarations preserve the historical whole-block pair benchmarks.  They
do not by themselves satisfy the paper's top-down L3 architectural-block
integration contract, which also requires model-derived activation coverage,
complete return values, and a layer-level saved-state contract.

A grouping package, not a declaration. See ``level1/__init__.py``.
"""
