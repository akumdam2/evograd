"""Level 3 — architectural blocks: a whole decoder layer or protein-model
block, where the saved-state decision becomes a property of the block rather
than of one kernel.

A grouping package, not a declaration. See ``level1/__init__.py``.
"""
