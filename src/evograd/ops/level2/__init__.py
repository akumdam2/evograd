"""Level 2 — fused operators: compositions that occur in real training,
evaluated on whether an implementation can optimize across the operator
boundary while preserving autograd semantics.

A grouping package, not a declaration. See ``level1/__init__.py``.
"""
